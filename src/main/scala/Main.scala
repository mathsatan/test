import com.kruchkov.numbers.{Diophantine, PrimeFactor}

object Main {
  def main(args: Array[String]): Unit = {
    /*val result = Diophantine.eval(17)
    result.foreach(i =>
      println(s"(${i._1}, ${i._2})")
    )*/
    /*val result = Diophantine.comb(List(PrimeFactor(3, 1), PrimeFactor(4, 2), PrimeFactor(5, 1)), (Nil, Nil))
    result.foreach(i =>
      println(s"(${i._1}, ${i._2})")
    )*/

    //Dioph.go()
    /*val s = Single.getInstance()

    new Thread(new Runnable {
      override def run(): Unit = {
        s.write(s"aaa")
      }
    }).start()
    s.write(s"mmm")*/
    /*val p = Monad.g2()
    p match {
      case Success(x) => println(x)
      case Failure(e) => println(e.getMessage)
    }*/
  }
}